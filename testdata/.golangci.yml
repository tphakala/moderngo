version: "2"
linters:
  default: none
  enable:
    - gocritic
  settings:
    gocritic:
      enabled-checks:
        - ruleguard
      settings:
        ruleguard:
          rules: "${config-path}/../*.go"
          failOn: "all"
